<!DOCTYPE html>
<html>
  <head>
    <title>Error Token</title>
    <style>
      body {
        background-image: url(http://localhost/Viktorina.live/Servers/images/background/VK_pasw2.png);
        background-position: center;
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      #errorMessage {
        text-align: center;
        background-color: #ffffff;
        background: rgba(95, 90, 90, 0.6);
        border-radius: 5px;
        box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
        padding: 60px;
        max-width: 400px;
      }
      #errorMessage p {
        color: #d9534f;
        font-size: 1.5em;
        font-weight: bold;
      }

      #countdown {
        font-weight: bold;
        font-size: 1.1em;
        /* color: #14354b; NEVEIKI */
      }

      #seconds {
        color: #5c2f2d;
      }
    </style>
  </head>
  <body>
    <div id="errorMessage"><% if (errorMessage) { %>
      <p class="error-message"><%= errorMessage %></p><% } else { %>
      <p>Iškilo nenumatyta problema, prašome pabandyti iš naujo.</p>
      <p>Jei nepavyksta pasikeisti slaptažodžio, prašome pranešti el. paštu: <%= yourEmailAddress %></p><% } %>
      <p id="countdown">Perkėlimas po <span id="seconds">8</span> sekundžių.</p>
    </div>

    <!-- <script>
      window.onload = function () {
        const errorMessageDiv = document.getElementById("errorMessage")
        errorMessageDiv.style.display = "block"

        let secondsLeft = 8
        const countdownElement = document.getElementById("seconds")

        function updateCountdown() {
          countdownElement.textContent = secondsLeft
          secondsLeft--

          if (secondsLeft < 0) {
            // window.location.href = "http://viktorina.live/d_regilogi.php"
            window.location.href = "http://localhost/Viktorina.live/d_regilogi.php"
          } else {
            setTimeout(updateCountdown, 1000)
          }
        }

        updateCountdown()
      }
    </script> -->
  </body>
</html>
